#include <stdio.h>
#include "combat.h"

void start_combat(Player *p, Enemy *e) {
    (void)p; /* silence unused-parameter warnings until this is built */
    (void)e;
    printf("\n[Combat system not built yet - coming in a later step]\n\n");
}