#include <stdio.h>
#include "save.h"

void save_game(const Player *p) {
    (void)p;
    printf("\n[Save system not built yet - coming in a later step]\n\n");
}

int load_game(Player *p) {
    (void)p;
    printf("\n[Load system not built yet - coming in a later step]\n\n");
    return 0;
}